<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{titre}}</title>
</head>
<body>
    <p>{{ burgerS.nom }}</p>
    <p>{{ burgerS.description }}</p>
    <p>{{ burgerS.prix }}</p>
</body>
</html>